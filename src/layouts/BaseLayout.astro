---
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import ThemeScript from '../components/ThemeScript.astro';
import '../styles/global.css'; // Import CSS in the frontmatter

interface Props {
  title: string;
  description?: string;
}

const { 
  title, 
  description = 'My personal website'
} = Astro.props;

const currentPath = Astro.url.pathname;
---

<!doctype html>
<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta charset="UTF-8" />
    <meta name="description" content={description} />

    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">

    <title>{title}</title>

    {/* Theme handling scripts */}
    <ThemeScript />
  </head>
  
  <body class="relative overflow-x-hidden min-h-screen text-content">
    {/* Grid Pattern - Fixed Position */}
    <div aria-hidden="true" class="pointer-events-none fixed inset-0 z-0">
      <svg class="w-full h-full opacity-20 dark:opacity-20 text-border-subtle dark:text-border" width="100%" height="100%">
        <defs>
          <pattern id="grid" width="20" height="20" patternUnits="userSpaceOnUse">
            <path d="M 40 0 L 0 0 0 40" fill="none" stroke="currentColor" stroke-width="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid)" />
      </svg>
    </div>

    {/* Blobs - Fixed Position */}
    <div aria-hidden="true" class="pointer-events-none fixed inset-0 z-10">
      <div class="absolute -top-40 -left-40 w-[18rem] h-[18rem] sm:w-[40rem] sm:h-[40rem] max-w-screen max-h-screen bg-theme-200 dark:bg-theme-900 rounded-full blur-3xl opacity-20"></div>
      <div class="absolute -bottom-40 -right-40 w-[18rem] h-[18rem] sm:w-[40rem] sm:h-[40rem] max-w-screen max-h-screen bg-accent-200 dark:bg-accent-900 rounded-full blur-3xl opacity-20"></div>
    </div>

    {/* Main Content Wrapper */}
    <div class="relative z-20">
      <Navbar currentPath={currentPath} />
      
      <main class="mt-24 flex-grow ">
        <slot />
      </main>
      
      <Footer />
    </div>
  </body>
</html>